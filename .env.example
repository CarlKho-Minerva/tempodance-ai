# Optional. The app remains fully usable in deterministic demo mode without keys.
FIREWORKS_API_KEY=
FIREWORKS_MODEL=
FIREWORKS_BASE_URL=https://api.fireworks.ai/inference/v1
TEMPO_ANALYZER_PROVIDER=local

# Local pose runtime.
TEMPO_POSE_MODEL=yolo11n-pose.pt
TEMPO_DEVICE=auto
TEMPO_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
